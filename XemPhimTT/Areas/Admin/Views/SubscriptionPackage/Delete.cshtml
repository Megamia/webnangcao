@model XemPhimTT.Models.SubscriptionPackage

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Gói Đăng Ký</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <span class="control-label col-md-2">Tên gói</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.name)
        </dd>

        <dt>
            <span class="control-label col-md-2">Giá</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.price)
        </dd>

        <dt>
            <span class="control-label col-md-2">Thời hạn (Ngày)</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.expiry)
        </dd>

        <dt>
            <span class="control-label col-md-2">Mô tả</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.description)
        </dd>

    </dl>

    @using (Html.BeginForm("Remove", "SubscriptionPackage", FormMethod.Post)) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(Model=>Model.subscriptionpackageid)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
