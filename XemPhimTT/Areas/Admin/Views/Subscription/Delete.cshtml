@model XemPhimTT.Models.Subscription

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Subscription</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <span>Người dùng</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.User.username)
        </dd>

        <dt>
            <span>Ngày đăng ký</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.startdate)
        </dd>

        <dt>
            <span>Ngày hết hạn</span>)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.enddate)
        </dd>

        <dt>
            <span>Trang thái</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.state)
        </dd>

        <dt>
            <span>Gói đăng ký</span>
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SubscriptionPackage.name)
        </dd>

    </dl>

    @using (Html.BeginForm("Remove", "Subscription", FormMethod.Post)) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(Model=>Model.subscriptionid)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
